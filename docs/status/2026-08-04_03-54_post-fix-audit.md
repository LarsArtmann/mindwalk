# Status Report: Crush Integration — Post-Fix Audit

> **RESOLVED:** All actionable items in this report have been addressed. See the Resolution section at the bottom for details.

**Date:** 2026-08-04 03:54
**Session goal:** Self-review the crush integration work, find what's broken, fix critical bugs, document everything

---

## A) FULLY DONE

### Go Data Model (committed `bed3e8e`)

- `model.TraceSession.Provider` — provider field on trace session
- `model.SessionMeta` — `Provider`, `PromptTokens`, `CompletionTokens`, `Cost` fields
- `model.Event.ProviderExecuted` — server-side execution flag
- `model.AgentLinkMethodCrushAgentID` — crush-specific link constant
- `adapter.ToolCall.ProviderExecuted` — threaded through `BuildEvent`

### Crush Adapter Enrichment (committed `bed3e8e`, `2c926b6`)

- Provider tracking (first non-null wins) + `model-switch` marks on mid-session changes
- Token usage + cost from sessions table → `SessionMeta`
- Exact read observability via `read_files` table (catches missing table, falls back to estimated)
- Finish reason marks: `error`, `content_filter`, `canceled`, `max_tokens` → `finish-reason` marks
- Thinking marks with duration from reasoning timestamps, fallback to `finished_at - created_at`
- Shell command events: bang-mode → deduplicated bash exec events
- Provider-executed flag: `toolCallData.ProviderExecuted` → event
- Crush link constant replaces misnomer codex constant

### Backward Compatibility Hardening (committed `801c01e` + uncommitted fixes)

- `schemaColumns` struct + `probeSchema()` detects `sessions.cost` and `messages.finished_at`
- Dynamic `build*Query()` functions substitute `0 AS cost` / `0 AS finished_at` when columns absent
- Schema probed once per `sqlHandle` creation, cached on struct
- Old DBs no longer crash with "SQL logic error: no such column"
- Verified with `TestOldSchemaListSessionsDoesNotCrash` + `TestOldSchemaParseDoesNotCrash`

### Frontend (committed `2c926b6`, reformatted by auto-git)

- TypeScript types: all new fields optional with `?`
- SessionRail: provider, token counts, cost in session rows
- HUD: provider shown next to model
- Timeline: `MARK_LABEL` entries + legend glyphs for thinking + finish-reason
- CSS: color variables + strip-mark + legend-glyph styles for all new mark types

### Schema Mirrors (committed + uncommitted reformatting)

- `trace.schema.json`: `provider`, `providerExecuted`, new mark enum values
- `agent-graph.schema.json`: `crush-agent-id` in linkMethod enum

### Tests (committed + uncommitted)

- 11 new adapter tests covering: provider, model-switch, read_files observability, finish-reason marks (4 reasons), thinking marks with duration, usage+cost, shell command events + dedup, old schema backward compat, thinking fallback duration
- All tests pass: `go test ./... -count=1`

---

## B) PARTIALLY DONE

### Uncommitted working tree changes

The following changes exist in the working tree but are NOT committed:

1. **`internal/adapter/crush/adapter_test.go`** — 3 new tests (`TestOldSchemaListSessionsDoesNotCrash`, `TestOldSchemaParseDoesNotCrash`, `TestThinkingMarkUsesMessageDuration`)
2. **`internal/adapter/crush/sessions.go`** — dynamic query builder + schema probing + finished_at fallback duration
3. **`internal/adapter/crush/agents.go`** — updated 3 query call sites to use dynamic builders
4. **Schema files** — reformatted + content changes
5. **Web source files** — reformatted (tabs → 2-space indent by auto-git daemon or another session)
6. **Static assets** — rebuilt by another process

### HUD `providerExecuted` integration

- `model.Event.ProviderExecuted` is populated in Go and typed in TypeScript
- But the HUD's "no file targets" warning does NOT check `providerExecuted` — it still says "check that the session's working directory matches" even when events are provider-executed
- The plan called for the warning to say "provider-executed tool" instead

### `model-switch` in Timeline legend

- CSS class `.strip-mark.model-switch` exists
- But the Timeline legend does not include a `model-switch` entry (only thinking and finish-reason were added)

---

## C) NOT STARTED

- `files` table → diff visualization (deferred — separate project)
- `todos` column → task progress UI (deferred — no UI surface)
- `updated_at` on messages → retry detection (deferred — low value)
- MCP/hooks/skills visibility (deferred — no citymap representation)
- Session grouping by project in the rail sidebar (ROADMAP item, not addressed)

---

## D) TOTALLY FUCKED UP

### D1: Uncommitted critical fixes at risk

The backward compatibility fix (dynamic query builders, schema probing) is **uncommitted in the working tree**. The auto-git daemon committed an intermediate version in `801c01e` that included the schema detection + shared helpers, but my subsequent fix — wiring the dynamic `build*Query()` functions into all call sites in `agents.go` and the `finished_at` fallback in `sessions.go` — sits uncommitted. If the auto-git daemon fires again it may commit these in a confusing state mixed with the web reformatting.

**Status:** Working tree is consistent and all tests pass, but the commit boundary is messy.

### D2: Web source files mass-reformatted

ALL web source files were reformatted from tab indentation to 2-space indentation. This was NOT my change — it appears to be from the auto-git daemon or another session. This creates a massive diff that obscures the actual content changes. The reformatting is not wrong per se (the project may have been moving to 2-space), but it makes review nearly impossible — every file shows as 100% changed.

**Status:** Content changes survived, but the diff is unreviewable.

### D3: `insertMessage` test helper doesn't insert `finished_at`

The shared `insertMessage` helper (line 106) still doesn't set `finished_at`. Tests that need it (like `TestThinkingMarkUsesMessageDuration`) use raw `db.Exec` instead. This is not a bug but a code smell — the helper should be updated or a new helper added.

---

## E) WHAT WE SHOULD IMPROVE

1. **Commit the uncommitted backward compat fixes IMMEDIATELY** — they're the most critical changes and they're sitting in the working tree exposed to auto-git interference
2. **Wire `providerExecuted` into HUD warning** — the field exists end-to-end but the UI doesn't use it
3. **Add `model-switch` to Timeline legend** — CSS exists but legend entry is missing
4. **Suppress zero-value display in SessionRail** — `0/0 tok · $0.00` is shown for every session with no usage data
5. **Update test fixture DB** — the committed `testdata/crush/crush.db` has all-zero tokens/cost and no `read_files` table
6. **Update `insertMessage` helper** to optionally set `finished_at` and `provider`
7. **The web reformatting should be acknowledged or reverted** — it creates a massive noise diff
8. **The read_files observability override is fragile** — it post-hoc overrides `ComputeStats` instead of threading the grade through

---

## F) Next 50 Things To Do

### Critical (fix now)

1. Commit the uncommitted backward compat fixes (dynamic queries + agents.go call sites + finished_at fallback)
2. Wire `providerExecuted` into HUD warning message
3. Add `model-switch` glyph to Timeline legend
4. Suppress `0/0 tok` and `$0.00` in SessionRail when tokens/cost are zero
5. Decide on web reformatting: commit it or revert it

### High value

6. Update committed test fixture DB with non-zero tokens, cost, and `read_files` table
7. Add test: shell command event appears in full trace pipeline
8. Add test: provider_executed events have no file targets
9. Add test: `model-switch` mark emitted on provider change
10. Update `insertMessage` helper to accept optional `finished_at` and `provider`
11. Update AGENTS.md to document new provider/usage/cost fields and mark types
12. Update ROADMAP.md to mark provider_executed and cross-check items resolved
13. Refactor read_files observability: pass grade through `ComputeStats` instead of post-hoc override
14. Add per-message duration to event metadata (not just thinking marks)
15. Add cost badge in HUD when cost > 0

### Polish

16. Add hover tooltip for thinking marks showing full text
17. Differentiate error (red) vs content_filter (orange) finish-reason glyph colors
18. Add token economics summary in HUD (prompt/completion/total)
19. Add provider icon in SessionRail
20. Animate thinking marks with subtle CSS pulse
21. Add session quality score widget using finish-reason marks as input
22. Extract `formatTokens` into shared `web/src/format.ts`
23. Move `truncateNote` to `internal/textutil`
24. Add `docs/crush-schema-mapping.md` table (DB column → model field)
25. Add `docs/mark-types.md` reference

### Architecture

26. Extract query-building logic into a `queryBuilder` struct
27. Consider `SchemaVersion` type that gates query construction
28. Consider whether `ProviderExecuted` belongs on `Event` or richer `ToolCall` type
29. Add a thinking lane in Timeline for visual duration bars
30. Consider grouping sessions by provider in the rail

### Testing

31. Add E2E test: full Parse → Trace → Frontend with all new fields
32. Add E2E test: old schema DB produces valid trace
33. Add property test: mark seq numbers are monotonically non-decreasing
34. Add test: `queryReadFiles` handles database errors gracefully
35. Add benchmark: Parse performance with/without read_files
36. Add test: concurrent Parse calls on same DB don't deadlock
37. Add test: shell command events get correct timestamps
38. Add test: provider_executed events have empty targets
39. Add test: model-switch note format is stable
40. Add test: fixture DB parses with exact observability path (after fixture update)
41. Add test: thinking mark with 0 duration shows "thinking:" prefix
42. Add test: finish-reason mark note includes message when present
43. Add test: truncateNote edge cases (empty, exact, multi-byte)
44. Add test: dynamic query builders produce valid SQL for both schema variants

### Documentation

45. Document new mark types in judge evidence pipeline
46. Add CHANGELOG entry for crush integration
47. Document backward compatibility approach
48. Update `docs/dynamic-rubric-evaluation.md` about new mark types
49. Add `schema/README.md` explaining new optional fields
50. Consider whether finish-reason marks should affect judge rubric criteria

---

## G) Questions

1. **Should the web reformatting (tabs → 2-space) be committed or reverted?** Every `.tsx`/`.ts` file was reformatted by the auto-git daemon or another session. Committing it creates a massive noise diff but makes the working tree clean; reverting it restores reviewability but fights whatever process did it. I can't tell if this was intentional or accidental.

2. **Should the uncommitted Go fixes (dynamic query builders in agents.go, finished_at fallback, 3 regression tests) be committed separately from the web reformatting?** They're logically independent changes but sit in the same working tree. The auto-git daemon could mix them at any time.

3. **The `801c01e` commit bundled my backward compat fix with shared adapter helper extraction and server tests from another session. Should this be considered one atomic unit, or should future commits try to separate concerns more cleanly?** The commit message says "one indivisible contract" but it actually contains three independent changes.

---

## Resolution (2026-08-04)

The section B "partially done" gaps both shipped in the sprint that followed:

- ~~HUD `providerExecuted` not wired~~ → done (CHANGELOG `[Unreleased] > Fixed`).
- ~~`model-switch` legend missing~~ → done (CHANGELOG `[Unreleased] > Fixed`).
- ~~`read_files` post-hoc override is fragile~~ → refactored: `ComputeStats`
  now takes a `readsSignal` parameter (CHANGELOG `[Unreleased] > Changed`).

The section F 50-item brainstorm: shipped items are in `CHANGELOG.md`
`[Unreleased]`; open bounded items are in `TODO_LIST.md`; long-term ideas
(`files`-table diff viewer, thinking lane, quality score) are in `ROADMAP.md`.
Section C deferred items (`files` table, `todos` column, MCP visibility)
remain deferred.
